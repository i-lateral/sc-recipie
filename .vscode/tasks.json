{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "DB Build",
            "type": "shell",
            "command": "./vendor/bin/sake dev/build flush=1",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}